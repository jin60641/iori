<!doctype html>
<html>
	<head>
		<% include ./header %>
		<% include ./../css/setting %>
		<script>
			<%
				if( locals.page == undefined || page.length == 0 ){
					page = "account"
				}
			%>
			page = "<%= page %>";
			
			settings = "<%= settings %>".replace(/&#34;/g,'"');
		</script>
		<script src="/js/setting.js"></script>
		<% include ./footer %>
	</head>
	<body>
	</body>
</html>
